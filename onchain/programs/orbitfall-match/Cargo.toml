[package]
name = "orbitfall-match"
version = "0.1.0"
description = "ORBITFALL staked 1v1 match escrow (devnet)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "orbitfall_match"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.31.1"
